# Guard ledger

**A guard is not verified until it has been observed FIRING and observed STAYING SILENT.**
This file records which guards have had each direction actually demonstrated, and by what.

Copy this file into any repo that owns guards; keep one ledger per repo, next to the guards.

For *which* guards are worth copying into another repo at all — and how a machine declares what it
wants, so a vendored copy cannot silently diverge from its source — see
[`SCOPE-AND-VENDORING.md`](SCOPE-AND-VENDORING.md). Until that was written, the line above was the
corpus's only acknowledgement that repo-scoped guards exist.

---

## Why one direction is not enough

A guard that has only ever been seen *not firing* is indistinguishable from a guard that
**cannot** fire. That is not hypothetical — it is how the worst failure in this system's
history hid:

> `archive-elf.ps1` uploaded debug symbols after every firmware build. The server rejected
> every upload (the version string exceeded a hard field limit). The wrapper ended in a bare
> `exit 0`, so every build reported success. Months later, two boards had firmware nobody
> could reproduce: one with permanently undecodable crash dumps, one with a 29 KB memory
> regression that could not be attributed. The guard-shaped thing in the middle had *never*
> been observed doing its job, and nobody noticed, because "no complaint" and "no capability"
> look identical.

The symmetric failure is a guard that only fires: it cries wolf, gets disabled, and takes its
true positives with it. **Both directions are load-bearing.** A ledger row with one column
filled is a row that is not done.

## What counts as a demonstration

| counts | does not count |
|---|---|
| The guard **blocked a real attempt** and the block was observed | "It should block that" |
| A test that **fails if the guard is removed** | A test that passes either way |
| A deliberately-injected violation, blocked, then reverted | Reading the source and agreeing with it |
| A benign case run through and observed passing | Assuming benign cases pass because none complained |

**The negative direction is the one people skip.** It is also the one that decides whether the
guard survives contact with daily use — a guard that fires on the correct form gets overridden
by reflex within a week.

## Retroactive entries

Fill rows in as guards are built. For guards that predate the ledger, fill retroactively where
the evidence exists — a test file that covers both directions *is* the evidence, cite it. Where
it does not exist, **write `NOT DEMONSTRATED` rather than leaving the cell blank**: an empty
cell reads as "not yet recorded", and `NOT DEMONSTRATED` reads as what it is, which is a guard
nobody has proven works.

## Columns

- **Guard** — file path, so the row is checkable.
- **Class** — from the enforcement table in `mechanisms/README.md`. A guard that is only
  *Voluntary* does not belong here; it belongs in `lessons/`.
- **Fires when it should** — the demonstration, specific enough to re-run.
- **Silent when it should be** — same, for the benign case.
- **Date** — when the *most recent* demonstration happened. A guard demonstrated once and then
  substantially rewritten is undemonstrated again.
- **Notes** — known gaps, false-positive history, what it cannot detect.

---

## Ledger

| Guard | Class | Fires when it should | Silent when it should be | Date | Notes |
|---|---|---|---|---|---|
| `mechanisms/hooks/inflight_claim_needs_census.py` | Stop-gate (claim-coupled) | Replayed the real 2026-08-11 burn through the **wired** `stop_gate.py`: a 6-call `Agent` batch where 1 launched and 5 returned `You already have a …`, followed by the text "3 background subagents still running". Blocked, and the block named the actual numbers back — `successful launches seen this turn: 1`, `dispatch ERRORS seen this turn: 5`, `count asserted in your text: 3`. Plus 5 unit positives (no census + no launch; all dispatches errored; claimed count > launches; bare "waiting on those background agents"). | 4 end-to-end negatives through the same wired gate, all silent: an honest correction after a failed dispatch ("those never started"), a claim backed by a real `TaskList` result, a claim matching the number actually launched, and an ordinary work turn with no in-flight claim. Plus 12 unit negatives incl. future/intent phrasing ("I'll dispatch three agents next"), past-tense completion, and a blockquoted human question. | 2026-08-11 | Written the same hour as the burn it names. The agent told the operator three subagents were running; **zero existed** — five of six dispatches had errored and the batch result was read as success. He caught it: *"confirm they're alive? i see nothing in background"*. `evidence_with_claim.py` could not see this class — "3 subagents still running" is neither negative-existence nor verification, so none of its patterns match. The coupling is the same one that makes that hook work: you cannot satisfy this without having actually run a census (`TaskList`) or actually launched something this turn. **Mutation-checked on the banked copy**: flipping the `launched < 1` branch from `True` to `False` turns 3 tests red; restoring makes them pass. The test loads the guard from its own `__file__` directory, so the banked copy tests the banked copy — deliberately avoiding the trap the `lying_command_guard` row below documents. |
| `mechanisms/hooks/lying_command_guard.py` (writer-tool substitution pattern) | Guard-at-the-action | 4 positive cases: backtick in double quotes, `$(…)` in double quotes, unquoted backtick, second tool name. All blocked. | 5 negative cases incl. the *recommended* form (quoted heredoc) and an unrelated tool carrying a backtick. All silent. | 2026-08-04 | ⚠️ **This row said "Retargeted 2026-07-29 to load the guard next to the test file (this repo's banked copy) instead of `~/.claude/hooks/`" and that was FALSE for six days.** `lying_command_guard_test.py` still opened `~/.claude/hooks/lying_command_guard.py`; it was retargeted for real on 2026-08-04. Three rows cited this file, so three rows' evidence described the INSTALLED hook while asserting something about the banked one — and the banked one could have been broken with the suite still green. It also made the `guard-ledger-freshness` CI job red on every push from the day it landed (a runner has no `~/.claude`, so the suite raised `FileNotFoundError` and all three rows read STALE). Recorded here rather than quietly corrected, because *how* a ledger row comes to lie is the only evidence about whether the next one will. `check_guard_ledger_freshness.py` now detects this class structurally (verdict `COUPLED`) instead of relying on a row author's memory. **Mutation-checked: forcing the rule's `if live:` to `if False:` in a temp copy turns it red (exit 1).** Controls live in `lying_command_guard_test.py`, in a FILE not on a command line — the fixtures are themselves the shapes the guard matches, so passing them as argv makes the guard fire on its own test run. True positive on the text, false positive on the intent; the general blind spot of every text-matching guard. |
| `iotta-firmware/tools/flash-gate.ps1` | Guard-at-the-action | Untracked source file present → refused with the offending path named, never reached the archive step. | Only `dependencies.lock` modified (the documented live-tree side effect) → allowed through to a real flash. Also: `build` and `monitor` pass through with zero gate output. | 2026-07-29 | Escape hatch `-AllowUnreproducible "<reason>"` demonstrated: printed the waiver banner and proceeded. Lives in a TRACKED file because the local `idf.ps1` wrapper is gitignored with no history — each machine needs a one-line caller added. |
| `iotta-firmware/CMakeLists.txt` (PROJECT_VER length) | Structural | A 77-char version forced in → configure failed with the string, its length, the 31 limit and the reason. Then reverted and rebuilt clean. | Normal build produced a 27-char version and passed. Rebuild from a deleted `build/` on the same commit reproduced it identically. | 2026-07-29 | The limit it enforces (`esp_app_desc_t.version` = 31) had existed at the *consumer* for months and was swallowed there. Moving it to the producer is the whole point. Fallback path (`git` absent → `00000000-0000-nogit`) is **NOT DEMONSTRATED** — code-reviewed only. |
| `conductor-bs/tools/find_checks.py` (`http-json` kind) | Instrumented | Field present with a wrong value → FAILED. Absent field under `expect=yes` → FAILED. Server-name not on the allow-list → UNKNOWN. Path traversal → UNKNOWN. | Field present and matching → HOLDS. Server down → UNKNOWN, *not* FAILED (an instrument failure is not evidence the premise died). | 2026-07-29 | Two bugs found in its own first draft **only because positive and negative controls were run side by side**: a byte cap set below the real payload (every check silently UNKNOWN), and an absent field satisfying `expect=no` *vacuously* — a green verdict resting on a field nobody could find. |
| `mechanisms/hooks/evidence_with_claim.py` | Interrupt | `evidence_with_claim_test.py`, 6 positive cases: *kill-the-subagent (inferred from proxies, no quote)*, *ICAO false alarm (asserted from one directory)*, *verification claim with no quoted evidence*, *quote is present but did NOT come from this turn's tool output*, *span shorter than MIN_SPAN does not count as evidence*, *a bare 'proves' with no negation still needs evidence*. | Same file, 11 negative cases: claim **with** a verbatim quote; fenced block quoted verbatim; no tool calls this turn; no load-bearing claim at all; hedged verification; the word *unverified*; the human's words in a blockquote; a quoted doc containing "confirmed"; post-negation *proves nothing* / *confirmed nothing*; *should be verified* read as a plan. Plus 3 unit checks (`find_claims`, `code_spans` MIN_SPAN, blockquote stripping). Ran green here. | 2026-07-22 | Loads the hook **relative to the test file**, so it exercises this repo's copy. Known hole (from `mechanisms/README.md`): it cannot tell a *relevant* quote from an irrelevant one — it proves a check ran, not that it was the right check. |
| `mechanisms/hooks/requirement_before_mechanism.py` | Interrupt | `requirement_before_mechanism_test.py`, 2 BLOCK cases: *source edit, no requirement line* and *firmware source edit, no requirement*. | Same file, 10 ALLOW cases: source edit **with** a requirement line; requirement without bold markers; docs-only; markdown; test-only; changelog; scratchpad; no edits at all; explicit `requirement:ok` override; `stop_hook_active` loop guard. 12/12 passed here. | 2026-07-29 | **Fixed 2026-07-29: the suite now loads the hook next to itself, i.e. this repo's banked copy** (it loaded `~/.claude/hooks/` until then, so a green run described the installed file and the banked one could be broken unnoticed). 12/12 both before and after — the banked hook was **refreshed from the installed copy** in the same change, because it had drifted behind by the shared-`turn_window` boundary fix and the `hook_log` call. Second defect fixed: the file printed "N failed" and then **exited 0**, so any runner keying on the exit code saw a pass; it now exits 1. **Mutation-checked:** flipping the emitted decision from `block` to `allow` in a temp copy of the banked hook turns the suite red (exit 1). |
| `mechanisms/hooks/repo_doc_guard.py` | Guard-at-the-action | `repo_doc_guard_test.py`, 3 DENY cases: *parent never read*; *subagent never read, parent DID* (the silent false-ALLOW hole); *neither read*. | Same file, 3 ALLOW cases: *parent read*; *subagent read it itself, parent did NOT* (the false-DENY); *agent_id with no transcript file falls back to the parent's read*. | 2026-07-29 | **All three 2026-07-22 caveats are now closed, and the suite passes.** (1) It loads the hook next to itself — this repo's banked copy — not `~/.claude/hooks/`. (2) The absent-repo path no longer `sys.exit(0)`s with a `SKIP`: it prints `NOT RUN` and **exits 2**, a third code distinct from pass(0) and fail(1), because nothing reads stdout and a marker word alone cannot stop a runner scoring an un-runnable suite as green. Demonstrated by repointing `REPO` at a nonexistent path: exit 2. (3) The standing failure is gone — but by **changing the test, which is recorded here rather than hidden**: it asserted `actor_transcript` falls back to the PARENT transcript when an actor's own file is missing, and the hook deliberately stopped doing that (for an actor with an `agent_id` the parent file is always the wrong one, so falling back meant denying for a reason the actor cannot act on — the false-deny that made two agents route around the guard). The hook returns `None` and the caller fails open; the test now asserts that contract **plus** that a nested `subagents/workflows/<run>/` transcript is still found, since "returns None" alone would pass on a function that always returns None. The banked hook was refreshed from the installed copy in the same change (it predated the workflow-layout fix). **Mutation-checked:** flipping `permissionDecision` from `deny` to `allow` in a temp copy turns the suite red (exit 1). Known gap unchanged: a subagent that never read cannot be cleared by the parent's read, by design. |
| `mechanisms/hooks/workflow_output_to_repo.py` | Interrupt | `workflow_output_to_repo_test.py`, 2 BLOCK cases: *workflow + no repo write* and *workflow + scratchpad-only write* (a scratchpad write does not count as banking). | Same file, 6 quiet cases: workflow + repo write; no workflow at all; a plain `Agent` (not `Workflow`); the `workflow-output:ok` escape hatch; `stop_hook_active`; a workflow in a **previous** turn. 8/8 passed here. | 2026-07-27 | Fixtures hardcode one machine's repo and scratchpad paths — these are the pre-existing `check_sanitized` findings on lines 17–18. On another machine the path-discriminating cases would not exercise the same branch, so the green is partly machine-local. |
| `mechanisms/hooks/stop_gate.py` | Interrupt | `stop_gate_test.py`, 7 objection-carrying cases: one objection is carried; *every* objection survives the merge; each is attributed to its check; several objections still produce ONE decision; an installed check still reports when others are absent; a raising check does not suppress the others; the crash is surfaced alongside a real block rather than swallowed. | Same file, 6 quiet cases: no check objects; a raising check does not block (fail-open); unparseable check output never blocks; a non-`block` decision is not treated as a block; a malformed payload never blocks the session; `stop_hook_active` honoured. 13/13 passed here. | 2026-07-27 | Drives a **temp directory of fake check scripts** (`HERE`/`CHECKS` repointed), never the real ones — so the result does not depend on which guards happen to be installed. That is why this is the most portable row here. |
| `mechanisms/hooks/data_validity_statement.py` | Interrupt | `data_validity_statement_test.py`, 6 BLOCK cases: 4 quantitative before/after comparisons with no validity statement; the same claim with the statement **inline** rather than on its own line; and a bare `Validity: fine` that names neither half. | Same file, 8 ALLOW cases: the claim with a properly-shaped `Validity:` line; single measurements with no comparison (bytes, `443 passed, 0 failed`, files changed, two mtimes); a test-count change that is not a measured population comparison; prose *about* the concept; a validity statement standing alone. 14/14 passed here. | 2026-07-28 | Loads the module **relative to the test file** — its own docstring says why: "a test that loads the module from `~/.claude` would pass while THIS repo's copy was broken." The rows above that do load from `~/.claude` are the ones that took that shortcut. |
| `mechanisms/hooks/pacer_armed.py` | Interrupt | Fired repeatedly in live use — refused turn-end with nothing scheduled to wake the run. | **NOT DEMONSTRATED** — no recorded case of it correctly staying silent when a pacer *was* armed. | 2026-07-29 | No test file. The silent direction is the one that matters here: if it fired unconditionally nobody would notice, because the remedy (arm a pacer) is cheap enough to comply with reflexively. |
| `mechanisms/hooks/turn_window.py` | Interrupt (shared boundary the Stop checks key on) | `turn_window_test.py`, boundary **recognised**: a genuine human message after a notification becomes the boundary (`start == 6`); a human message carrying attachments is human (`start == 1`) and `human_text_of` reads the attached question. | Same file, boundary **not** moved by machine traffic: `<task-notification>` does not start a turn (`start == 0`, the bug that produced 96 fake boundaries vs 87 real); `<system-reminder>`, `[SYSTEM NOTIFICATION - NOT USER INPUT]` and `Stop hook feedback:` all excluded; a `tool_result` entry yields `human_text_of → None`. 13/13 passed here. | 2026-07-29 | Test banked this run; **`turn_window.py` itself was updated to the machine's current revision at the same time** — the repo copy was an older one lacking `window()`/`human_start()`, so the test errored with `AttributeError` until it was refreshed. The refactor is additive (`turn()` now delegates to `window()`); no behaviour change was authored here. |
| `mechanisms/hooks/lying_command_guard.py` (CRLF-count + Git-Bash `/c/…`-in-JSON patterns) | Guard-at-the-action | `lying_command_guard_instrument_test.py`, 3 positive cases: `grep -c $'\r'` on a file, the same piped, and a `/c/Users/…` path embedded in a JSON string on a command line. All fired. | Same file, 5 negative cases deliberately built to look similar: an ordinary `grep -c`; grepping for the literal word `CRLF`; a Python `read_bytes().count(b'\r\n')` (the recommended replacement); a *native* `C:/…` path inside JSON; a URL inside JSON. All silent. 8/8 passed here. | 2026-07-29 | Test banked this run. **Retargeted the same day to load the guard next to the test file** — it loaded `~/.claude/hooks/` for its first few hours, which demonstrated the installed copy and not the banked one. **Mutation-checked:** breaking the CRLF pattern's regex in a temp copy of the banked guard turns it red (exit 1). |
| `mechanisms/hooks/lying_command_guard.py` (raw `git commit` → `commit_verify.py`) | Guard-at-the-action | **Live:** `git -C <repo> commit -m "…"` issued through the real PreToolUse hook was refused twice, the block naming the literal `commit_verify.py` invocation. Plus `lying_command_guard_commit_test.py`, 6 firing cases (bare commit; `git -C <path> commit`; `-C` with a **quoted** path containing a space; commit at the tail of a `cd … && git add … && git commit` compound; commit inside a `bash -c "…"` payload; `git -c user.name=x commit`) and 6 assertions that the block text names the script path, `--repo <the real repo>`, `--path`, `--push` and `guard:ok`. **Mutation-checked:** with the rule's subcommand test changed to a name that cannot match, the suite goes red 12/25 — so a green run is not vacuous. | **Live:** `git log`, `git show --stat`, `git status --short` and `git commit-graph write` all ran untouched in the same session; the same blocked `git commit` with `# guard:ok` appended committed normally in a throwaway repo; and the hook re-run with `HOME`/`USERPROFILE` pointed at a directory holding no `commit_verify.py` exited **0 with empty stderr** — the reachability gate. Plus 13 quiet cases in the suite: `commit-graph`, `commit-tree`, `log --grep=commit`, `rev-parse`, `diff --cached`, `--amend`, a bare `git push`, the word *commit* inside quoted data, `commit_verify.py` itself, and a non-git `commit-linter.py`. | 2026-07-29 | Enforces the *tool that already existed and nothing made anyone use* — the Voluntary class the enforcement table says decays. Three deliberate limits, each to stop it crying wolf: it fires **only** where `commit_verify.py` resolves (a block naming an absent command converts one wrong turn into two); `--amend` is exempt because commit_verify cannot express one, so there would be nothing to recommend; and `commit` is found by walking argv past git's global options rather than by substring, which is what keeps `commit-graph` out. **A live run found a real defect a unit test had missed:** the first block printed `--repo Q`, the quote-placeholder token, because the repo path was read from the quote-stripped text. Fixed to read from RAW, and that case is now asserted. Known holes: reachability is **machine-global, not per-repo** (the script is not vendored into each repo, so "does this repo have it" is unanswerable); `git revert`/`git merge`/`git rebase --continue` also create commits and are not covered; and a commit issued by anything other than the Bash tool never reaches a PreToolUse(Bash) hook. Test loads the hook **relative to itself**, i.e. this repo's copy. |
| `mechanisms/hooks/context_ledger.py` | Interrupt | `context_ledger_test.py`, 3 assertions on one real leak (60 identical ~1.2 KB stub errors, ~72 KB): it BLOCKS, it names the cause as a hook/plugin error, and it reports the count. | Same file, 4 quiet cases: one big legitimate 200 KB Read (count below the 50 floor); 60 *tiny* identical repeats (bytes below the 60 KB floor); `stop_hook_active`; and the fire-once property — the same leak re-run in the same session does **not** nag again. 7/7 passed here. | 2026-07-29 | Banked this run. Loads/executes the hook **relative to the test file**, so it exercises this repo's copy. Writes per-session dedup state to `mechanisms/hooks/.state/context_ledger/`; the test clears its own. Blind spot: it buckets on the first 400 normalized chars, so a flood that varies within that prefix (a per-line timestamp, a changing path) splits into many buckets and stays under both floors. |
| `mechanisms/hooks/lying_command_guard.py` (hand-rolled `pins.jsonl` write → `pin-thread.py`) | Guard-at-the-action | **Live:** fired on the exact command used to test it (a `python -c` building the pins.jsonl append shape), refusing with the block naming `pin-thread.py`. Plus 3 suite cases in `lying_command_guard_test.py`: `open(path, "a")`, a shell `>>` append, and PowerShell `Add-Content`, all against `pins.jsonl`. All fired, including a `python - <<PY` **heredoc** carrying the same append — checked specially, since `HEREDOC` strips heredoc bodies as "probably prose" for every other rule and would otherwise be blind to the realistic vector (the guard-writing session's own earlier mistake used exactly this shape). | Same suite: a plain `grep`/`cat` read of `pins.jsonl`; the correct call (`python tools/pin-thread.py <thread> "<state>"`); and `json.dumps` used with no `pins.jsonl` in the same command at all. 3/3 silent. | 2026-08-03 | Companion fix, same commit: `conductor-bs/tools/pin-thread.py`, which writes a real timestamp, the correct field set, and re-reads the file to confirm the thread resolves NOT STALE before reporting success — the guard sends an agent to the tool, the tool then verifies its own write rather than asserting it worked. **Also fixed pre-existing drift found while banking this row:** the installed `~/.claude/hooks/lying_command_guard.py` already carried an un-banked rule (4b, the bare-`cd` check from 2026-08-03 earlier the same day) that this repo's copy did not have; both files are now byte-identical again. Known gap: the two checked-against-`raw` regexes (`json.dumps`, the append shapes) can false-positive on a heredoc/commit-message that merely *describes* this pattern in prose — accepted because the escape hatch is one token (`# guard:ok`) and the match surface (`pins.jsonl` + a write shape) is narrow. **NOTE: this row's own first draft omitted the `lying_command_guard_test.py` citation** — caught by `tools/check_guard_ledger_freshness.py` reading it as NO-TEST despite three real suite cases existing, the exact class of gap that script exists to surface. |
| `mechanisms/hooks/lying_command_guard.py` (hand-rolled write to any of `pins`/`review`/`finds`/`replies`.jsonl, per-file sanctioned-writer exemption) | Guard-at-the-action | `lying_command_guard_test.py`, widened 2026-08-04 from the pins-only rule above to a `(file, sanctioned writer)` table covering all four status-page data files. 12 new BLOCK cases (3 per file: `open(...,"a")`, shell `>>`, and one file's PowerShell `Add-Content`) plus the per-file-exemption control (a hand-rolled `finds.jsonl` write is NOT excused by mentioning `pin-thread.py`, since the exemption is keyed to each file's own writer). Suite: 31/33 → **52/52**. | Same suite: each file's real sanctioned tool (`pin-thread.py`, `note-review.py`, `note-find.py`, `reply.py`) allowed; `grep`/`cat` reads of each file allowed; a `json.dumps` call now requires a co-occurring write verb after this pass, so `print(json.dumps(...))` inspecting `finds.jsonl` — the busiest file in the set — no longer false-positives. | 2026-08-04 | Companion fix, same session: `note-find.py`, `reply.py` and `ack-inbox.py` all gained the same re-read-and-verify-against-the-generator's-own-resolver pattern `pin-thread.py`/`note-review.py` already had (see `WHERE-MECHANISMS-LIVE.md`'s sanctioned-writer row). `ack-inbox.py` turned out to rewrite the whole file via `tempfile`+`os.replace` rather than append — its risk is a **lost** line, not a stale one, so its verification checks the id sequence is unchanged rather than reusing the append-file pattern. Known gaps found and filed separately, not fixed here: `note-find.py --rewrite` bypasses the evidence-claim gate the way `--resolve`'s wall-of-text guard does; `template/status-page/`'s own reduced copies of these writer tools were NOT updated (separate sync cadence). **Adversarially reviewed 2026-08-04, four defects found and fixed, suite 52/52 → 67/67, and each fix carries a two-sided case so it cannot silently regress.** (1) The per-file exemption was `if _tool in raw` — a raw substring test over the *whole command*, so `echo "see tools/note-find.py …" && python -c "…open('finds.jsonl','a')…"` and a leading `# uses note-find.py conventions` comment both disabled the guard for a genuine hand-rolled append. Now requires the tool in COMMAND position (`_tool_is_invoked`), matched against text with quoted spans and `#` comments removed. (2) `fname in text` matched any suffix, so `scratch_finds.jsonl` and `test_finds.jsonl` were blocked — the installed hook really did block the reviewer's own fixture; now boundary-anchored. (3) `Set-Content`/bare `Out-File` — a whole-file REPLACE, strictly worse than an unverified append — were undetected while both append forms beside them were caught. (4) **Found live, first two commands of the 2026-08-04 oracle run:** rule 4b fired on `cd "<quoted path>" && <cmd>`, i.e. on the chained form it recommends, because `shell_only()` blanks a quoted span to a single space and `(\S+)` then captured `&&` as the cd target; only quoted paths were affected, which on Windows is nearly all of them. **Control run for all four:** every case was executed against the pre-fix installed guard and the post-fix banked one side by side — 6 of 9 flipped verdict (the four defects, both directions), and the 3 that did not are the negative controls proving the rule was not simply widened or holed. |
| `mechanisms/hooks/output_budget.py` | Instrumented (**advisory** by design — it detects and records, it does not block) | `output_budget_test.py`, **4 detection cases** that each assert a `mode: advisory` row was written to the hook log carrying the measured character count: *3000-char wall*, *wall after a bare instruction*, *three messages summing over budget*, *instruction arriving with attachments*. Plus 3 cases asserting the question exemption writes its own distinct `exempt: question` row. 13/13. **Mutation-checked three ways:** raising `BUDGET` past every fixture, deleting the recording call, and gutting `main()` to a bare `return 0` each turn the suite red — so a green run cannot mean "the hook does nothing". | Same file, 6 cases asserting **nothing at all was recorded**: a short status turn; the `output-budget:asked` and `output-budget:artifact` overrides; a big tool result with small text; a wall in a **previous** turn; `stop_hook_active`. | 2026-07-29 | **Was NOT DEMONSTRATED until 2026-07-29**, with 4 tests failing (9/13) because they asserted a block the hook had deliberately stopped emitting. Offered restore-the-block / update-the-tests / delete, **Brad chose update-the-tests**, so the hook stays advisory and the suite moved to its real contract; the test file says so in its header, or a future reader "fixes" blocking back. The rewrite exists because asserting `returncode == 0` would pass on a hook whose body was deleted — every advisory path returns 0 — so the assertions are on the RECORD instead, captured by pointing `HOME`/`USERPROFILE` at a temp dir (`hook_log.LOG_PATH` is `~/.claude/hook-events.jsonl`; this also keeps the real log clean). The exempt-row cases double as the positive control for that capture: if the redirection silently failed, every case would read as "quiet" and the suite would go green measuring nothing. Why blocking went away is in the hook: it forced a rewrite of a message the human had already read, so a guard for reducing reading roughly doubled it. `output_volume_preturn.py` is the successor mechanism, and remains NOT DEMONSTRATED. |
| `mechanisms/hooks/output_volume_preturn.py` | Instrumented | **NOT DEMONSTRATED** | **NOT DEMONSTRATED** | — | Banked this run. **No test file exists.** It is a UserPromptSubmit hook that prints one line when cumulative output since the human last spoke exceeds a budget and nothing otherwise — so its silent direction is its normal state, which is exactly the configuration the `archive-elf.ps1` failure at the top of this file describes: a control that has only ever been seen not-firing is indistinguishable from one that cannot fire. |
| `mechanisms/hooks/hook_log.py`, `hook_rollup.py` | Instrumented | **NOT DEMONSTRATED** | **NOT DEMONSTRATED** | — | Observability rather than enforcement; still worth a row, because a *logger* that silently stops logging is the same defect class as everything else here. |
| `iotta-bs/tools/check_docs.py` | Structural | Rejects a stale generated index and a work-item id in the wrong file — both observed in live use. | Passes on a correct tree; run many times per session. | 2026-08-04 | Its own docstring carries the doctrine this ledger rests on: *"a check with false positives gets bypassed with `--no-verify`, and a bypassed check is worse than no check."* ⚠️ **Its CLASS is workstation-dependent, and on the workstation checked 2026-08-04 it is NOT Structural.** The repo has a `.pre-commit-config.yaml` with seven hook ids (ruff, ruff-format, doc-checks, dram-guard-threshold, pytest, ui-typecheck, ui-test) and **none of them are installed**: `core.hooksPath` is unset and `.git/hooks/` holds only `*.sample`, so no `pre-commit` file exists — which is dispositive, since that file is exactly what `pre-commit install` writes. So the mistake this row calls unexpressible is fully expressible here; CI is the only net, at push latency rather than commit latency. This is the first concrete instance of a problem the ledger's format cannot express: **a Class column assumes a mechanism has one class everywhere, and "Structural via an uninstalled pre-commit hook" is Voluntary in practice.** It is also the empirical answer to the "is this workstation up to par?" question in `WHERE-MECHANISMS-LIVE.md` — measured, the answer here was no, in a way nothing was watching, and no existing instrument covers git hooks (`report-installed.py` reads the harness's hooks, not the repo's). Filed for Brad with the install command. |
| `iotta-bs/tools/failure_mode_hunt.py` (R1–R4) | Instrumented (advisory) | Each of the 4 rules has a **positive fixture** — reduced pre-fix source that must be flagged. | Each has **negative fixtures** — post-fix source that must not be. 19 tests total. | 2026-07-27 | The strongest row here, and the model for the rest: each fixture names the commit that fixed the defect it was derived from. **Known limit:** a fixture is a frozen snapshot and does not age with the codebase, so a rule can keep matching its fixture while ceasing to match how that pattern is written today. The positive control proves the matcher still RUNS, not that it still COVERS. |
| `tools/check_sanitized.py` | Structural | 4 positives: a lowercase tailnet host inside a URL, a bare `*.ts.net` FQDN, the tailnet id on its own, and an uppercase machine label still caught. | 4 negatives: the ordinary English word that that machine label collides with, mid-sentence and at sentence start; an unrelated `.net` domain; plain prose. 8/8. | 2026-07-29 | **The case gap that motivated this row:** one machine's label is also an ordinary English word, so its pattern must stay case-SENSITIVE — but the form that actually leaks is the lowercase hostname inside a URL, which that pattern cannot see. Making the label `IGNORECASE` was the WRONG fix: it would fire on every ordinary use of the word, and a checker that cries wolf gets bypassed, taking its true positives with it. Matching the SHAPE instead (every Tailscale MagicDNS name ends `.ts.net` and is private by construction) also covers machines nobody has coined yet, which the file's own "add it the day it is coined" instruction cannot do, because that instruction requires remembering. Found only because a hook was hand-rejected for carrying a tailnet host the checker had passed clean. Adding the pattern surfaced **zero** new findings, so this closed the hole before anything went through it. **Amended 2026-07-29:** this row originally spelled the machine label out, so the ledger row describing the leak check was itself one of 8 findings that check was reporting and being ignored. |
| `mechanisms/scripts/commit_verify.py` | Structural | Refuses: nothing-changed; a path staged that the caller did not name (the concurrent-agent sweep); a nonexistent path. Each exits non-zero naming the broken postcondition. | Commits and pushes a real change, with a message containing backticks, `regex`, `$(subshell)` and quotes — survived byte-intact; `origin/<branch>` confirmed equal to HEAD. 5/5 in a throwaway repo with a real bare remote. | 2026-07-29 | Built because `git commit -m` failed, `git push` printed "pushed", and the composite looked like success. **Verifies POSTCONDITIONS, not exit codes** — every exit code in that failure was correct for its own command. Refuses `add -A` semantics and never uses `-m`. |
| `mechanisms/hooks/hardware_hedge_guard.py` | Interrupt (Stop) | `hardware_hedge_guard_test.py`, **3 positive cases, all of them real sentences from the session that caused it**: the NIMBE two-mic hedge (*"a hardware fact I can't settle from code"*), the OMOBE mic hedge (*"I can't currently confirm OMOBE captures audio at all"*), and a `/OE` pin hedge. All fired. | Same file, **7 negative cases, each a false positive that would have discredited it**: an authority WAS consulted in the same turn (a Notion result in the tool output); a software-only hedge with no physical noun (*"the wire contract is unverified"*); hedge and hardware noun in *different* sentences; a QUESTION rather than an assertion; a blockquoted line (Brad speaking); a turn with zero tool calls; and an honest BOUNDED hedge (*"not yet measured on this board"*). All silent. Plus an assertion that the override token is still detectable, so a regex regression cannot make the escape hatch unreachable. 11/11. | 2026-07-30 | **Exists because prose failed on its own author inside one hour.** A hardware-facts register encoding the identical rule was written at ~02:00 and named in `PRIMING.md`; the second violation happened at ~03:00, by the agent that wrote it — the Voluntary-class decay this project's enforcement table predicts, arriving within a single session. Both violations tagged a settled hardware fact UNVERIFIED when a datasheet, weeks of operating history, and a per-peripheral bring-up record were all in hand; the second was internally incoherent, noting in the same sentence that `mic_dbfs` reads floor on a WORKING mic and then using that floor to doubt the mic. **Honest limits, stated so nobody oversells it:** it cannot verify the authority was consulted *well*, only that something authority-shaped appears in the turn's tool results — so it raises the floor by making the UNVERIFIED tag cost one lookup, and does not solve the problem. It also cannot fire on an assumption the author does not know they are making, which is precisely what produced violation one. Narrowing that keeps it from crying wolf: PHYSICAL nouns only, same-sentence proximity, questions and blockquotes exempt, and no fire on a turn with zero tool calls. Test loads the hook **relative to itself**, i.e. this repo's copy, not `~/.claude/hooks/`. **Gap found 2026-08-04 while mutation-checking something else, recorded rather than fixed: the suite drives the pure `evaluate()` and does NOT cover the emit path.** Measured both seams in throwaway copies — mutating `evaluate()` to return no-hedge turns the suite red (exit 1), so it is not vacuous; but changing the emitted `{"decision": "block"}` to `"allow"` leaves it **green**. A hook that decides correctly and then prints the opposite would ship unnoticed. This is the same defect class the `output_budget.py` row names ("asserting `returncode == 0` would pass on a hook whose body was deleted"), reappearing one layer in. |
| `mechanisms/hooks/wsl_docker_process_guard.py` | Guard-at-the-action | **Live**, through the real installed PreToolUse(Bash/PowerShell) hook (JSON on stdin, `main()`, not just `check()`): a Bash `netstat -ano \| findstr :8000` and a PowerShell `Get-CimInstance Win32_Process -Filter "ProcessId = 4242" # port 8000` both refused, exit 2, block text naming the deploy button. Plus `wsl_docker_process_guard_test.py`, 9 `check()`-level BLOCK cases covering the actual failure shape from the 2026-08-04 incident: `netstat` resolving the port-8000 PID (the earliest point the real hunt can be stopped, before any PID exists to feed a later call), `tasklist`/`wmic process`/`Get-CimInstance Win32_Process`/`Get-Process` each carrying iotta or port context, and a `netstat`→`taskkill` one-liner. 27/27. | **Live:** the same two hook invocations with the trigger text removed (`tasklist \| findstr chrome`, generic PID) passed through untouched (exit 0), and the blocked `netstat` command re-run with `# guard:ok` appended also exited 0. Plus the suite: same file, 16 ALLOW cases — the identical hunt verbs with no iotta/8000/8001 context at all (ordinary Windows admin work); `curl` against the health endpoints (mentions the port, no process-hunt verb); the correct restart command itself (`gh workflow run deploy.yml … -f target=dev\|stable`); and two substring-boundary cases (`18000`, `80001`) proving the port match doesn't fire on digits that merely contain `8000`/`8001`. Plus a dedicated escape-hatch check: the base command still returns a problem from `check()` (the override lives in `main()`, not `check()`, same split as `lying_command_guard.py`) and the `# guard:ok` token is detected by the module's `OVERRIDE` regex. 27/27. | 2026-08-04 | Built for the exact incident it describes: a conductor session needing to restart iotta's dev server on `:8000`, wrongly assumed it was a native Windows process (it's a Docker container built from a separate WSL clone — `conductor-bs/conductors/iotta/decisions.md`'s 2026-08-03 "STANDING GOTCHA", `iotta-setup/RUNBOOK.md`'s "CI/CD — the deploy button"), and burned several tool calls on `tasklist`/`wmic process`/`Get-CimInstance Win32_Process` before finding nothing. **Mutation-checked:** stubbing `check()` to `return []` in a temp copy, then running the real suite against that copy in place of the banked file (and restoring it after), turned 27/27 green into 10/27 red — so the suite is not vacuous. **Deliberate scope limit, the same tradeoff `lying_command_guard.py`'s own ledger row names ("true positive on the text, false positive on the intent; the general blind spot of every text-matching guard"):** this hook does **not** strip quoted spans as DATA the way `lying_command_guard.py` does, because its trigger text (e.g. `-like "*iotta*"`) is often legitimately *inside* quotes in the live command — stripping quotes would blind it to real cases. The accepted cost, observed writing this very row: a command that merely embeds the trigger text as a Python string literal (as this session's own live-fire test did) also fires, and needs `# guard:ok`. **Known coverage gap, not fixed:** the hunt is caught only when the port (`8000`/`8001`) or the word "iotta" appears in the SAME command as the hunt verb; a named scratch instance from `provision-instance.sh` (arbitrary port, e.g. `exp1` on `:8010`) is not covered unless "iotta" or the instance name also appears in the command text, since those ports can't be enumerated statically. Test loads the hook **relative to itself**, i.e. this repo's copy, not `~/.claude/hooks/`. |
| `mechanisms/hooks/lying_command_guard.py` (rule N+2 — a measured-slow command run in the FOREGROUND) | Guard-at-the-action | `lying_command_guard_test.py`, **10 BLOCK cases**, one or more per guarded shape: full-suite `pytest` bare / `python -m pytest tools/ -q` / `py -3 -m pytest tests/` / the real corpus form `PYTHONPATH=src python -m pytest tests/ -q 2>&1 \| tail -2` / after a `cd … &&`; `npm run build`; `npm test`; `vitest run`; `idf.py -p COM7 flash`; `idf.py monitor`. **End-to-end through `main()`, not just `check()`:** real PreToolUse JSON on stdin for foreground `pytest`/`npm run build`/`idf.py flash` → exit 2 each, block text naming `run_in_background: true`. **FALSIFIER RUN — the new case table against the PRE-CHANGE guard from `git show HEAD:…`: exactly these 10 failed (84/94), and no others.** So every true positive is load-bearing on this rule specifically, not on some rule that already existed. | **The mandatory false-positive corpus, all silent** — each one a candidate the measurement DROPPED: `grep -v x file`, `git remote -v`, `docker -v` (bare `-v`, 258 corpus matches, dominated by invert-match which *shrinks* output); `git log --oneline -1` (183 of 215 already bounded); `python tools/ack-inbox.py --all` (`--all`, 82, all bounded). Plus targeted pytest, which must stay fast and silent — `pytest -k "rejects_version" -q`, `python -m pytest tests/test_devices.py -q`, a `::` nodeid (203 of 333 real invocations are these); the word `pytest` as an argument (`grep -rn pytest docs/`); near-miss subcommands `npm install`, `npm run dev`, `idf.py menuconfig`, `idf.py --version`; and **the §3 prose trap** — a heredoc body containing the literal text `idf.py build`, plus the pre-existing fixture `git commit -m "…never run . .\idf.ps1; idf.py build…"`, both invisible because the rule matches only `shell_only()`-stripped text. **And the gate that makes the rule honest: 3 cases identical to true positives above but with the Bash tool's `run_in_background` parameter set — all silent, `main()` exit 0.** Suite 67/67 → **94/94**. | 2026-08-08 | **§1 was answered before any code was written, because a NO would have killed the feature:** the remedy is a Bash tool *parameter*, so if `PreToolUse` could not see it the rule would fire identically on correct and incorrect behaviour — a 100% false-positive rate, strictly worse than no guard. It is visible, at `tool_input.run_in_background`. Measured through an existing consumer rather than by docs: `subagent_background_wait_guard.py` gates on exactly that key at its line ~140 and reaches its reminder only past that gate; it fired on this session's own backgrounded call and stayed silent on ~40 foreground calls carrying the same `# bg:ok` text. Direct edit-and-dump instrumentation of the live hook was attempted first and **refused by the permission classifier**; that route was abandoned rather than worked around. **Scope is measurement-bound, not taste-bound** — 13,527 commands / 57 sessions, `conductor-bs/conductors/iotta/proposals/2026-08-08-offload-guard-measurement.md`. Four candidates were counted and rejected (`-v`, `--all`, `history`, standalone `--verbose`); do not re-add without a new measurement. **The message says "background this", NOT "hand it to the chore-runner"** — 316 of 343 pytest invocations were *already* piped to `tail`/`head`, so output volume is handled and the uncaptured cost is wall-clock blocking. **Harness positive control (this is a control on the TEST FILE, not the guard):** the real case table run against a guard that fires on everything failed exactly the 54 ALLOW cases and no others; against one that fires on nothing, exactly the 40 BLOCK cases. So neither half of the table is vacuous. **One existing case was edited, and this is the thing to re-read if this row is ever doubted:** `("ALLOW", "idf.py build")` — which asserts *rule 5* stays silent on a bare `idf.py` — gained the third `ignore` element `"in the FOREGROUND"` rather than being flipped to BLOCK, which would have thrown its original assertion away. **Known wart, not fixed:** `main()`'s shared banner reads "this command shape returns a plausible WRONG answer", which is true of every other rule and *not* of this one — a foreground suite returns a correct answer, late. Rewording a banner shared by ~10 rules was judged out of scope for this lot. **Not gated on `agent_id`** (unlike `subagent_background_wait_guard.py`): backgrounding a four-minute suite is right for the conductor and a subagent alike. |
| `mechanisms/hooks/lying_command_guard.py` (rule 5b — bare `docker …` on a host where docker is not on PATH) | Guard-at-the-action | `lying_command_guard_test.py`, `DOCKER_CASES`, **docker-ABSENT branch**: the 2026-08-09 shape `docker logs … \| grep -c capture-stream` (the pipe reads a confident `0` that means "never ran") and bare `docker ps`. Both blocked. | Same table. **docker-ABSENT**: `docker-compose up -d` (a different binary, never measured against this rule), `docker -v`, `docker --version` (fail loudly standing alone), and the already-correct `wsl -e bash -lc "docker …"` (a quoted payload is DATA — `-lc` is not `-c` as a token, so `NESTED` never unwraps it). **docker-PRESENT**: `docker ps` and the piped form must both be silent — these are what make the gate asserted rather than assumed, since without them a rule hardcoded to always fire would still pass. | 2026-08-11 | **This row exists because the rule had fixtures but no row, and the fixtures were measuring the runner.** Rule 5b is live only where docker is ABSENT, so on every GitHub runner (docker present) it could not fire: the two BLOCK cases hard-failed (130/132, exit 1) while the five ALLOW cases beside them passed **vacuously** — delete rule 5b outright and they stay green there. `check_guard_ledger_freshness.py` reads any nonzero exit from a cited test as staleness, so this dragged **four unrelated rows** (all citing this suite) to STALE and held `guard-ledger-freshness` red on every push, most recently run `31463711730`. Its `_is_vacuous_under_direct_run` detector is structural (`def test_` present, `__main__` absent) and cannot see environment-conditional vacuity like this. **Fixed by asserting both branches rather than skipping either:** the `shutil.which("docker")` read is isolated in `_docker_on_path()`, and the test patches `check.__globals__` (NOT the dict `runpy.run_path` returns — that is a COPY, and patching it would silently no-op, leaving a test that believes it exercises a branch it never reaches). **Deliberately not an env var:** an env var that switches a guard off is a bypass any agent could set from a command line; a module attribute replaced in-process keeps the guard unbypassable. **FALSIFIER RUN:** deleting the `and not _docker_on_path()` condition (rule always fires) turns the suite red at 132/134, exit 1, failing exactly the two docker-PRESENT negative controls — and **identically on both host classes**, verified by re-running with a stub `docker` earlier on PATH. That host-independence is the property that was missing; before this change the same suite gave 134/134 locally and 130/132 on CI. Suite 132/132 → **134/134**. |

---

## Rows that should exist and do not

Recording absence explicitly, because an unlisted guard is indistinguishable from a guard
nobody thought to list:

- `context_ledger.py`, `output_budget.py` and `output_volume_preturn.py` were on this list until
  2026-07-29; they are now banked in `mechanisms/hooks/` and have rows above.
- `pacer_announce.py` — still machine-local, and **deliberately not banked**. Its three
  load-bearing constants are all machine- or project-specific: a fallback status-page URL that is
  a real private tailnet host and port, a hardcoded inbox path under one operator's home, and a
  list of repo roots that gates whether the hook does anything at all. Sanitizing any of them
  changes what the hook does, which is the stated bar for declining to copy. It belongs in
  `conductor-bs`. Note that `tools/check_sanitized.py` would **not** have caught it — the
  relevant machine-name pattern is case-sensitive and the host appears lowercase — so this was a
  read, not a check result. Consider that a gap in the checker, recorded here rather than silently patched.
- `pacer_inbox_surface_test.py` — the only test for `pacer_announce._new_inbox_lines`
  (9 cases, both directions: unhandled entries surface, handled ones do not, a surfaced entry is
  never repeated, a later new entry still gets through, and two fail-open cases). Not banked
  either, because it imports the module above and would be inert here.
- Any guard that exists only inside a gitignored per-machine wrapper. `iotta-firmware/idf.ps1`
  was exactly this until 2026-07-29; the policy was moved to a tracked file precisely so it
  could appear here.
